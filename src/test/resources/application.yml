kestra:
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: local
    local:
      base-path: /tmp/unittest
  tasks:
    bigquery:
      project: "kestra-unit-test"
      dataset: "kestra_unit_test"
